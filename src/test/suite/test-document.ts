// A plain comment

// A multiline comment
// that is shorter than it needs
// to be

// A long comment that should be broken up into multiple lines. Getting to the actual line limit is a bit annoying though

/**
 * A docstring comment
 * that should be left along
 */

/* A multiline string that should be left alone for now but is long enough to get split across
 * multiple lines */
